<% @posts.each do |x| %>
  <div>
    <%= image_tag x.image, width: "800px", height: "500px" if x.image.attached? %>
    <br>
    <%= x.description %>
    <div class="link">
      <%= link_to "Edit", edit_post_path(x) %>
      <%= link_to "Show", x %>
    </div>
  </div>
<% end %>
